enum GameState {
  started,
  roundStarted,
  roundStopped,
  stopped,
  paused,
  voting,
  scores,
  stickExploded,
  connected,
  disconnected,
  disconnecting,
  stickPassingDone
}

enum GameMode {
  theme,
  emotion
}