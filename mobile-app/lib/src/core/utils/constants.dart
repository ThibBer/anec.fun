enum GameState {
  started,
  roundStarted,
  roundStopped,
  stopped,
  paused,
  voting,
  scores,
  stickExploded,
  connected,
  disconnected,
  disconnecting
}

enum GameMode {
  theme,
  emotion
}