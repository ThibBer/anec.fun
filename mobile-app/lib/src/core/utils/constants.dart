enum GameState {
  started,
  roundStarted,
  roundStopped,
  stopped,
  idle,
  voting,
  scores,
  stickExploded,
  disconnected,
  disconnecting,
  stickPassingDone
}

enum GameMode {
  theme,
  emotion
}